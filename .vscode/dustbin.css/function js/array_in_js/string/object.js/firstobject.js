// Create an object called student with name, age, and grade. Print all values.

// Add a new property hobby to an existing object.

// Change object value (city → Pokhara).

// Delete a key from object.

// Access key using bracket notation.

// Make a method called greet() inside an object.

// Loop through object using for...in.
 const student = {
     name :"prakriti koirala",
     age :15,
     grade : 45,

 }
 console.log(student.name);
 student. name = "swostika",
 console.log(student.name);
 delete student.age;
 console.log(student);